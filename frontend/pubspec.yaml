name: frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
    # Fíjate que 'http' está pegado al mismo margen que 'flutter'
  http: ^1.2.0

  # Es probable que también tengas esto debajo (déjalo como está)
  cupertino_icons: ^1.0.2
    


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/logo.png
