name: frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/logo.png

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  # La caja fuerte para guardar el token
  flutter_secure_storage: ^9.0.0 
  cupertino_icons: ^1.0.2
  device_info_plus: ^12.3.0
